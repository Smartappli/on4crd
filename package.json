{
  "name": "on4crd-e2e",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "selenium-webdriver": "^4.45.0"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "test:selenium": "node --test --test-concurrency=1 --test-timeout=240000 \"tests/selenium/*.test.js\""
  }
}
